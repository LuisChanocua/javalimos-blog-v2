# local = desarrollo con contenido local; empty = produccion/pre-produccion sin contenido demo.
VITE_CONTENT_SOURCE=local

# URL publica del sitio. En produccion usar https://javalimos.org.
VITE_SITE_URL=http://localhost:8080

# Server-only. No usar prefijo VITE_ para secretos ni credenciales.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=
